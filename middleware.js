// Middleware disabled — auth handled client-side by AuthGuard.jsx
// Re-enable once session cookie behaviour is confirmed in production.
export function middleware() {}
export const config = { matcher: [] };
